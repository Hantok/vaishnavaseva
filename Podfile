# Uncomment this line to define a global platform for your project
# platform :ios, '8.0'
# using Swift: removed for now as we need to support iOS 7 where swift frameworks are not supported so there is a linker error: embedded dylibs/frameworks are only supported on iOS 8.0 and later (@rpath/OAStackView.framework/OAStackView) for architecture armv7
# use_frameworks!

target 'Vaishnavaseva' do
pod "OAStackView"
end

target 'VaishnavasevaTests' do

end

target 'VaishnavasevaUITests' do

end

